export default function ShowInput({ val }) {
  return (
    <div style={{ border: '2px dashed green' }}>
      This the ShowInput components
      <br />
      Shared state Val is {val}
    </div>
  );
}
